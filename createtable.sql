CREATE TABLE IF NOT EXISTS test (
  id INT NOT NULL PRIMARY KEY, -- self auto_increment
  name VARCHAR,
  surname VARCHAR,
  birthdate VARCHAR,
  email VARCHAR,
  username VARCHAR,
  password VARCHAR
);