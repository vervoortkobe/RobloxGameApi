CREATE TABLE IF NOT EXISTS test (
  id INT PRIMARY KEY, -- self auto_increment
  name NOT NULL VARCHAR,
  surname VARCHAR,
  birthdate VARCHAR,
  email VARCHAR,
  username VARCHAR,
  password VARCHAR
);